<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:padding="24dp"
    android:layout_margin="100dp"  android:backgroundTint="@color/pink"
    android:background="@android:color/white"> <TextView
    android:id="@+id/createCharacterTv"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:text="Create new character"
    android:textSize="16sp"
    android:layout_marginBottom="12dp"
    android:textColor="@color/white"/>

    <TextView
        android:id="@+id/importCharacterTv"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Import character"
        android:textSize="16sp"
        android:layout_marginTop="12dp"
        android:textColor="@color/white"/>

</LinearLayout>